# Generated by orrbit setup. Edit as needed.
# Run: docker compose up -d

services:
  orrbit:
    build: .
    container_name: orrbit
    restart: unless-stopped
    ports:
      - "5000:5000"
      - "2222:2222"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - orrbit-data:/app/data
      # Mount your directories below (generated by setup wizard):
      # - /path/to/documents:/mnt/documents:ro
      # - /path/to/media:/mnt/media:ro

volumes:
  orrbit-data:
